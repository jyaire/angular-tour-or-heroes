import { BoldGreenTextDirective } from './bold-green-text.directive';

describe('BoldGreenTextDirective', () => {
  it('should create an instance', () => {
    const directive = new BoldGreenTextDirective();
    expect(directive).toBeTruthy();
  });
});
